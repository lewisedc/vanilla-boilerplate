{
  "name": "project-name-here",
  "scripts": {
    "start": "concurrently \"parcel watch src/*.html\" \"ws -d dist\"",
    "build": "parcel build src/*.html",
    "clean": "rm -rf dist && rm -rf .cache"
  },
  "devDependencies": {
    "autoprefixer": "^9.8.4",
    "concurrently": "^5.2.0",
    "local-web-server": "^4.2.1",
    "normalize.css": "^8.0.1",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.9"
  },
  "browserslist": [
    "defaults"
  ]
}
